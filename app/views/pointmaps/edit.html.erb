<h1>Editing Depthmap</h1>

<% form_for(@pointmap) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :object => f %>
  <%= f.submit 'Update' %>
<% end %>

<%= link_to 'Show', @pointmap %> |
<%= link_to 'Back', depthmaps_path %>
